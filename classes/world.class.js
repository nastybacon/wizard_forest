class World {
    // let brauch man bei Klassen auch nicht
    character = new Character(); // Character definieren
    enemies = [ // Gegner 
        new Enemy(),
        new Enemy(),
        new Enemy(),
    ];

    draw() {

    }
}